# Steam Readme Stats (Steam Widget)

Сервис для генерации **SVG-виджета** по Steam-профилю. Используется для вставки в GitHub README, документацию или любую страницу, которая умеет показывать картинки по URL.

Виджет забирает данные из **Steam Web API**, собирает их в один объект и рендерит в один SVG. Аватарка встраивается внутрь как `data:image/...`, поэтому GitHub / Vercel / Markdown её не блокируют. Если в Steam нет аватара, возвращается встроенная заглушка.

---

## Возможности

- 🔹 Аватар профиля (inline, без внешних запросов на стороне клиента)
- 🔹 Имя профиля (или своё через `?title=...`)
- 🔹 Уровень Steam
- 🔹 Количество игр
- 🔹 Количество друзей
- 🔹 Последняя сыгранная игра (многострочный рендер + авторасширение виджета)
- 🔹 Индикатор статуса (online / offline / away)
- 🔹 Локализация (`?lang=ru|en`)
- 🔹 Темы (`?theme=dark|light`)
- 🔹 Защита от ошибок Steam API (всегда возвращается валидный SVG)
- 🔹 In-memory кэш (10 минут) для разгрузки Steam

---

## Требования

- Node.js >= 18
- npm или pnpm
- действующий **Steam Web API Key**  
  Получить: https://steamcommunity.com/dev/apikey

---

## Установка

```bash
git clone https://github.com/nervan-iwnl/steam-readme-stats.git
cd steam-readme-stats
npm install
```

Создай `.env` в корне:

```env
STEAM_API_KEY=your_steam_api_key
PORT=3000
```

---

## Запуск

```bash
npm start
```

По умолчанию сервер слушает `http://localhost:3000`.

Проверка:

```text
http://localhost:3000/steam-widget/test
```

Этот эндпоинт не ходит в Steam и нужен для быстрой проверки верстки.

---

## Основной эндпоинт

```text
GET /steam-widget
```

### Query-параметры

| Параметр  | Обязат. | Описание |
|-----------|---------|----------|
| `steamid` | да*     | SteamID64 профиля. Если не передан — вернётся валидный SVG с заглушками. |
| `lang`    | нет     | `ru` или `en` (по умолчанию `en`). Меняет подписи (`Games`, `Friends`, ...). |
| `theme`   | нет     | `dark` (по умолчанию) или `light`. |
| `title`   | нет     | Переопределяет имя из Steam. Полезно, если в Steam ник грязный. |
| `width`   | нет     | Принудительная ширина SVG. Если не задана — виджет сам растянется под ник / игру. |

\* — если `steamid` не указан, сервис всё равно вернёт SVG, чтобы Markdown не ломался.

### Примеры

```text
http://localhost:3000/steam-widget?steamid=76561198801977286
http://localhost:3000/steam-widget?steamid=76561198801977286&lang=ru&theme=light
http://localhost:3000/steam-widget?steamid=76561198801977286&title=GIGACHAD&width=540
```

---

## Встраивание в GitHub README

```markdown
![Steam](https://<your-vercel-app>.vercel.app/steam-widget?steamid=76561198801977286&lang=ru&theme=dark)
```

**Почему это работает:**  
при рендере мы скачиваем аватар сами и встраиваем его в SVG в base64, а при ошибках подставляем свою svg-заглушку. GitHub не видит там внешних `<image href="https://...">` → не блокирует.

Если GitHub сильно кеширует — добавь бесполезный параметр:

```markdown
![Steam](https://<your-vercel-app>.vercel.app/steam-widget?steamid=7656...&t=123456)
```

---

## Деплой на Vercel

1. Залей репозиторий на GitHub.
2. Создай проект на https://vercel.com/
3. При импорте репозитория укажи переменную окружения:

   ```text
   STEAM_API_KEY=your_steam_api_key
   ```

4. Задеплой.

После этого виджет будет доступен по:

```text
https://<your-vercel-app>.vercel.app/steam-widget?steamid=...
```

---

## Структура проекта

```text
.
├── server.js               # входная точка (Express)
├── vercel.json             # конфиг для Vercel
├── src
│   ├── routes
│   │   └── widgetRoute.js  # /steam-widget, /steam-widget/test
│   ├── services
│   │   └── steamService.js # Steam API, кэш, avatar->base64, fallback
│   └── render
│       ├── renderTemplate.js # сборка SVG, авто-ширина, переносы
│       ├── svgText.js        # утилиты для измерения текста
│       └── strings.js        # ru/en строки
└── templates
    └── widgetTemplate.svg    # базовый SVG шаблон
```

---

## Особенности

- аватар **всегда** есть: либо реальный из Steam, либо встроенная svg-заглушка
- виджет старается не 500-ить: в маршруте логируем ошибку, но всё равно отдаём картинку
- кэш в памяти живёт, пока живёт инстанс (для README этого хватает)
- ширина может увеличиваться по двум причинам: длинный ник или длинное название игры

---
